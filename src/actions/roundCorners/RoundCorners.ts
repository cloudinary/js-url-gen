/**
 * Round one or more corners of an image or video.
 * @memberOf Actions
 * @namespace RoundCorners
 */

import radius from './radius';
import max from './max';

export {radius, max};
export default {
  radius,
  max
};
