/**
 * Round one or more corners of an image or video.
 * @memberOf Actions
 * @namespace RoundCorners
 */

import byRadius from './byRadius';
import max from './max';

export {byRadius, max};
export default {
  byRadius,
  max
};
