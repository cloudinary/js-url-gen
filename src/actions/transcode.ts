import {ITranscodeAction, Transcode, bitRate, audioCodec, audioFrequency, fps, fpsRange, keyframeInterval, streamingProfile, toAnimated, videoCodec}
  from "@cloudinary/transformation-builder/actions/transcode.js";

export {ITranscodeAction, Transcode, bitRate, audioCodec, audioFrequency, fps, fpsRange, keyframeInterval, streamingProfile, toAnimated, videoCodec};
